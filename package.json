{
  "name": "assignments-app",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "10.x",
    "yarn": "1.x"
  },
  "scripts": {
    "start": "node server/bin/www",
    "postinstall": "yarn install --prefix ./server",
    "heroku-postbuild": "cd client/ && yarn install && yarn install --only=dev --no-shrinkwrap && yarn build"
  },
  "cacheDirectories": [
    "client/node_modules",
    "server/node_modules"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/mdekalka/assignments.git"
  },
  "keywords": [
    "node",
    "heroku",
    "create-react-app",
    "react"
  ],
  "license": "MIT",
  "devDependencies": {}
}